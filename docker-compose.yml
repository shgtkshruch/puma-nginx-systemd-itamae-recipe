version: '3.0'
services:
  terraform:
    image: hashicorp/terraform:light
    volumes:
      - ./terraform:/terraform
    working_dir: /terraform
    env_file:
      - .env
    tty: true
    stdin_open: true
